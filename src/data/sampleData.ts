import { Machine, Worker } from '../types';
import { realisticMachines, realisticWorkers } from './knowledgeBase';

export const sampleMachines: Machine[] = realisticMachines;
export const sampleWorkers: Worker[] = realisticWorkers;
